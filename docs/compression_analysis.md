# Compression analysis

Compression guidance uses peak, RMS, crest factor, dynamic range, and frame-level loudness variation. Suggested ratio, attack, and release values can come from a trained model or the rule-based fallback.

Avoid adding compression when the analyzer reports limited dynamics. Preserve transient impact by adjusting attack and threshold while monitoring gain reduction.
