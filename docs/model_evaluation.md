# Model evaluation

Evaluation reports compare predicted parameters with held-out targets and create residual and predicted-versus-actual plots. Review aggregate metrics together with residual structure; a low average error can still hide bias at extreme settings.

Evaluation images and metrics are generated under `results/` and are not committed.
