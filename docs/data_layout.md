# Data layout

Raw and processed training audio belongs under `data/raw/` and `data/processed/`. Trained estimators belong under `models/`, and plots or metric reports belong under `results/`.

These generated or potentially large assets are excluded from version control. Source code and reproducible instructions remain tracked.
