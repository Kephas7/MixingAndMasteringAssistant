# Application usage

Start the UI with `streamlit run app.py`, upload a supported audio file, and choose an analysis workflow. Begin with the overview metrics before applying processing recommendations.

Use the interactive EQ and compression controls for auditioning. Exported previews are rendered as PCM WAV. Keep an untouched copy of the original mix because preview processing is intentionally non-destructive.
