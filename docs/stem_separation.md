# Stem separation

The stem workflow separates a mix into musical sources through Demucs. Processing time and memory use scale with track length and model size.

Separation artifacts are normal, especially around overlapping instruments and reverberation. Use isolated stems for diagnosis and targeted edits, then confirm decisions against the full mix.
