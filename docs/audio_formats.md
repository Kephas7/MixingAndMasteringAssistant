# Audio formats

WAV and FLAC are preferred because they avoid lossy decoding artifacts. MP3 is suitable for demonstrations but may bias high-frequency and transient analysis.

For consistent comparisons, use files with the same sample rate and bit depth. The application converts uploaded audio to a mono analysis signal; exported previews use 16-bit PCM WAV.
